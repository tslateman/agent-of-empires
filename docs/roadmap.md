# Roadmap

Planned features and improvements.

## Planned

- **Group rename**: Allow renaming a group by pressing `r` when a group is selected. Updates all sessions with that group path, including nested paths (e.g., `oldname/subgroup` -> `newname/subgroup`).
